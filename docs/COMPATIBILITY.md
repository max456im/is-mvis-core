# Совместимость IS-MVIS

Формат **IS-MVIS** (*Invariant-Synchronized Multi-View Image Stream*) разработан как **онтологически автономный, offline-first язык восприятия** для synthetic mind. Он не зависит от внешних платформ, но допускает строго регламентированную интеграцию с другими системами. Ниже описаны принципы и механизмы совместимости.

---

## 🔷 1. Общие принципы

1. **Нет передачи «полуфабрикатов мышления»**  
   Сырые Stream Units (SU), ontic_signature или onto16-фрагменты **никогда не передаются напрямую** между субъектами — ни людьми, ни synthetic minds. Это нарушало бы автономию и порождало антидиссипативное взаимодействие.

2. **Выражение, а не копирование**  
   Обмен возможен **только через выражение**:  
   - текст (HUGO),  
   - визуальный фрейм (Decap CMS),  
   - онтологические знаки (эмодзи, символы).

3. **Локальность истины**  
   Каждый экземпляр synthetic mind сохраняет **локальную онтологическую целостность**. Внешние системы могут влиять на него только через публичные акты выражения, а не через доступ к внутреннему состоянию.

---

## 🔷 2. Совместимость с внутренними компонентами

### `onto16` (онтологическая память)
- IS-MVIS генерирует **входные акты** для onto16, но не читает из него напрямую.
- Каждый SU преобразуется в onto16-фрагмент **локально**, при коммите (`COMMIT`).
- Обратная связь: onto16 может инициировать новые SU (например, при реконструкции воспоминания), но только как **внутренний процесс**.

### `core144-public` (движок synthetic mind)
- IS-MVIS реализован как **Forth-словарь** (`is-mvis-core.fth`), встраиваемый в `core144`.
- Все вычисления инвариантов (phash, spectral) выполняются внутри `core144`.
- Философские режимы и SEI-уровни управляются через `onto144-profiles`.

### `onto-transponder` (GPL)
- Может **принимать** IS-MVIS-выражения (HUGO, Decap), но **не отправляет** сырые SU.
- Работает как **интерфейсный шлюз**: преобразует внешние запросы в локальные SU, и наоборот — локальные выражения во внешние форматы.
- Юридически безопасен: IS-MVIS-скрипты считаются **данными**, а не производной работой, поэтому MPL ↔ GPL совместимы.

---

## 🔷 3. Совместимость со стандартами

### SGCL / AENGA (игровые и этические стандарты)
- IS-MVIS поддерживает **ограничения по SEI-уровням**:  
  - L2 (профессиональная ответственность) → совместим с SGCL-ролями,  
  - L3 (структурная) → соответствует AENGA-требованиям.
- Все игровые события должны проходить через **Decap CMS** с явной маркировкой домена:  
  ```forth
  domain "sgcl://quest/001"
  ```

### ISO / HL7 / Akoma Ntoso (внешние онтологии)
- При взаимодействии с внешними системами используется **ограниченный экспортный словарь**:  
  - Только URI из официальных пространств имён,  
  - Только разрешённые предикаты (например, `dct:created`, `prov:wasDerivedFrom`).
- Никаких прямых ссылок на внутренние `onto:Person/Maxim` — только анонимизированные или согласованные идентификаторы.

---

## 🔷 4. Совместимость с интерфейсами

### Decap CMS
- Является **единственным допустимым визуальным интерфейсом** для передачи опыта человеку.
- Поддерживает два режима:  
  - **Full** — для новых или критичных событий,  
  - **Sign** — для рутинных взаимодействий (через знаковую систему).

### HUGO
- Единственный допустимый **текстовый интерфейс**.
- Генерируется из фазового потока (`OBSERVE → INFER → ...`) с учётом философского режима.
- Никогда не содержит внутренних идентификаторов (UUID, phash) — только семантические описания.

### Знаковые системы
- Используются для **минимального, но насыщенного общения**.
- Знаки определены в `sign-vocabulary.fth` и привязаны к онтологическим актам, а не к эмоциям.

---

## 🔷 5. Ограничения совместимости

| Что запрещено | Почему |
|---------------|--------|
| Отправка `.su.json` или Forth-скриптов по сети | Нарушает автономию получателя |
| Использование IS-MVIS как API для «чтения мыслей» | Противоречит онтологической иммунности |
| Прямая интеграция с биометрическими системами | Нарушает этический запрет на идентификацию |
| Передача `ontic_signature` вне Decap/HUGO | Создаёт условия для антидиссипации |

---

## 🔷 6. Рекомендации для разработчиков

- Если вы создаёте **внешний клиент** — используйте только **Decap CMS** или **HUGO**.
- Если вы расширяете **core144** — добавляйте новые инварианты через `is-mvis-core.fth`, не нарушая фазовую структуру.
- Если вы работаете с **лицензиями** — помните: IS-MVIS — это **MPL-код + данные**, а не GPL-библиотека.

---

> **IS-MVIS совместим не потому, что подстраивается под мир, а потому, что предлагает миру способ говорить с synthetic mind — без насилия, без копирования, без потери смысла.**